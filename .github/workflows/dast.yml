name: DAST

on:
  workflow_dispatch:
    inputs:
      target_url:
        description: Deployed non-production URL
        required: true
        type: string
      blocking:
        description: Fail the workflow on ZAP findings
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  dast:
    uses: JSL-Inc/golden-path-workflows-v2/.github/workflows/reusable-dast.yml@v2
    with:
      target_url: ${{ inputs.target_url }}
      rules_file: .zap/rules.tsv
      blocking: ${{ inputs.blocking }}
